{
  "name": "koriyo-guard",
  "version": "1.0.0",
  "description": "화성시 산불감시 코리요 지킴이 (Capacitor + FastAPI)",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "prepare:www": "node scripts/prepare-www.js",
    "cap:sync": "npm run prepare:www && npx cap sync android",
    "cap:open": "npx cap open android",
    "serve": "npx --yes serve ."
  },
  "dependencies": {
    "@capacitor/android": "^7.4.2",
    "@capacitor/core": "^7.4.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.2"
  }
}
